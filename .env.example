# Copy to .env. These are the values that match the docker compose services
# when the backend runs on the host.

# asyncpg URL for the TimescaleDB instance
DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/sentinel

# Mosquitto broker the MQTT hub subscribes to
MQTT_HOST=localhost
